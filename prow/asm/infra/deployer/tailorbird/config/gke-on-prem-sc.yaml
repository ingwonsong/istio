apiVersion: sut.tailorbird.io/v1alpha1
kind: Rookery
metadata:
 name: asm-ci-onprem-sc
 namespace: tailorbird-users
spec:
 ttlSeconds: 28800 # 8 hours
 knests:
 - apiVersion: sut.tailorbird.io/v1alpha1
   kind: Knest
   metadata:
     name: asm-ci-onprem-sc-knest
   spec:
     clusters:
     - apiVersion: sut.tailorbird.io/v1alpha1
       kind: Cluster
       metadata:
         name: asm-ci-onprem-sc-cluster
       spec:
         provider: vsphere
         distribution: gke
         provisionerArgs:
           username: prowuser
           lbkind: "MetalLB"
           ipAllocationStrategy: static
           controlPlaneV2Enabled: "{{ .AdditionalParams.controlPlaneV2Enabled | default true }}"
           userAcnMode: "{{ .AdditionalParams.userAcnMode | default true }}"
           additionalNodes: "8"
           {{- if .Version }}
           gkectlVersion: "{{ .Version }}"
           {{- end }}
           {{- if .VersionPrefix }}
           gkectlVersionPrefix: "{{ .VersionPrefix }}"
           releaseMode: "true"
           useStagingBucket: "true"
           {{- end }}
           gkectlConfigSeedVersion: "v1"
           gkeConnectProject: "{{ .OnPremHubDevProject }}"
           stackdriverProjectId: "{{ .OnPremHubDevProject }}"
           # gkeConnectSA: "{{ .OnPremGkeConnectSA }}"
           vsphereVersion: "7.0"
           esxiVersion: "7.0"
